

extra_defs_src = files(['generate_defs_malgtk.cpp', 'generate_extra_defs.cpp'])

extra_defs_gen = executable('extra_defs_gen', extra_defs_src, link_with: libmalgtk, dependencies: libmalgtk_deps, include_directories: libmalgtk_inc)
